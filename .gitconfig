[user]
  # email is configured by local machine setup
  # email = user@example.com
  name = Kyle Ronning

[core]
  # brew install delta
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true  # use n and N to move between diff sections
  line-numbers = true

[merge]
    conflictstyle = zdiff3
